var usernameText = "unlucki"; // custom username
var timeQuoteText = "Alignment... Post Panic"; // custom quote inside time container. cutoff occurs at 3 lines in quote box. reccomended 2 or less lines in box
var greetQuoteText = "I live this life for my ancestors"; // custom quote next to profile picture
var greetQuoteColor = "#e5ebf0"; // enter hex code or rgba()
var preferredAppearance = false; // leave off for default colors. enable for blur.
var badgeColor = "#fff"; // enter hex or rgba() for custom color. enter none to hide badges completely
var badgeWidth = "24"; // changes badge width in px
var badgeHeight = "4"; // changes badge height in px
var hideDockBadges = false; // enable to only hide dock badges
var hideInfoBoxTime = false; // enable to hide time in info box
var hideMusicButton = false; // hides music button & container
var hideInfoBoxUsername = false; // hides username in info box
var hideProfilePicture = false; // hides profile picture. colors can only be changed with preferrendAppearance if enabled
var reset = false; // Resets the apps stored in the widget. Steps: 1. Enable switch 2. Confirm widget settings 3. Exit widget settings, then go back in 4. Disable switch
